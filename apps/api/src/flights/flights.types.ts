export type FlightSegmentView = { airlineCode: string; flightNumber: string; origin: string; destination: string; departureAt: string; arrivalAt: string; durationMinutes: number; aircraft?: string; departureTerminal?: string; arrivalTerminal?: string };
export type FlightOfferView = { id: string; provider: 'AMADEUS'; price: number; taxes: number; currency: string; cabin: string; bookingClass?: string; fareFamily?: string; refundable: boolean; stops: number; durationMinutes: number; layovers: Array<{ airport: string; minutes: number }>; segments: FlightSegmentView[]; airlineCode: string; airlineName?: string; baggage: { carryOn: string; checked: string }; seatsRemaining?: number; mealAvailable: boolean; carbonEmissionsKg?: number; fareRules?: string[]; score: number };
export type PriceCalendarDay = { date: string; lowestPrice: number; currency: string };
